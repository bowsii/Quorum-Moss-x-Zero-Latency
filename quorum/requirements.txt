# Quorum — Python dependencies
# All free-tier, no credit card required

# Core async & web
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Pydantic v2 (strict validation)
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Database
aiosqlite>=0.20.0
asyncpg>=0.29.0

# Semantic index — Moss / ChromaDB (in-process, no HTTP hop)
chromadb>=0.5.0
moss-sdk>=0.3.0

# Inference — primary: Groq
groq>=0.9.0

# Inference — structured output
instructor>=1.3.0
openai>=1.30.0  # instructor uses openai-compatible client

# Inference — failover: OpenRouter and HF (openai-compatible)
# No extra packages needed; openai client handles both

# Search
tavily-python>=0.3.0

# Transport: LiveKit
livekit>=0.11.0
livekit-api>=0.6.0

# Observability
opentelemetry-sdk>=1.24.0
opentelemetry-api>=1.24.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Profiling (py-spy is CLI, not a library import)
# Install separately: pip install py-spy

# HTTP client for async calls
httpx>=0.27.0
aiohttp>=3.9.0
